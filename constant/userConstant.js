// gender enum
const genderEnum = ["male", "female"];

// user role enum
const roleEnum = ["buyer", "seller"]

// province enum
const provinceEnum = ["province no 1", "madhesh", "bagmati", "gandaki", "lumbini", "karnali", "sudur pashchim"]

const addressEnum = ["street", "city", "province"]


module.exports = {
    genderEnum,
    roleEnum,
    provinceEnum,
    addressEnum
}